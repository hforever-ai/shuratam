# Dependencies
node_modules/

# DB credentials
config/db.php

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Tailwind build cache
.cache/

# API keys (safety layer)
safety/keys.json
safety/.rate_limits.json

# Audio files (deploy separately)
audio/
audio_output/

# Generated outputs (deploy separately via rsync)
outputs/
training/
player_static/
logs/

# learn-english/ source (templates, helpers, content) is tracked.
# Only the archived old structure stays out of git — local-only, won't deploy.
learn-english/_archive/

# Temp
*.log
nohup.out

# MCP server configs (contain API tokens)
.mcp.json
